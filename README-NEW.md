# Nano Banana AI 画布 - 前后端分离版本

## 🏗️ 项目架构

```
nano-banana-ai-canvas/
├── frontend/          # React 前端应用 (端口 3000)
│   ├── src/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── components/
│   ├── index.html
│   ├── package.json
│   └── vite.config.ts
├── backend/           # Node.js API 服务 (端口 3001)
│   ├── src/
│   │   ├── server.ts
│   │   ├── services/
│   │   ├── config/
│   │   └── utils/
│   ├── package.json
│   └── tsconfig.json
├── shared/           # 共享类型定义
│   └── types.ts
└── package.json      # 根项目配置
```

## 🚀 快速开始

### 1. 安装依赖
```bash
# 安装所有依赖（根目录、前端、后端）
npm run install:all
```

### 2. 配置环境变量
```bash
# 复制后端环境变量模板
cp backend/.env.example backend/.env

# 编辑配置文件
# 配置数据库连接、API 密钥等
```

### 3. 启动开发服务器
```bash
# 同时启动前后端开发服务器
npm run dev

# 或者分别启动
npm run dev:frontend  # 前端 (http://localhost:3000)
npm run dev:backend   # 后端 (http://localhost:3001)
```

## 📡 API 接口

### 健康检查
```
GET /api/health
```

### 图片管理
```
GET    /api/images          # 获取图片列表
POST   /api/images          # 创建新图片
PUT    /api/images/:id      # 更新图片信息
DELETE /api/images/:id      # 删除图片
```

### 配置管理
```
GET    /api/config          # 获取配置
POST   /api/config          # 保存配置
POST   /api/config/test     # 测试连接
```

### 数据库操作
```
GET    /api/database/status  # 数据库状态
POST   /api/database/init    # 初始化数据库
GET    /api/database/stats   # 数据库统计
```

## 🔧 技术栈

### 前端
- **React 19** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Lucide React** - 图标库

### 后端
- **Node.js** - 运行时
- **Express** - Web 框架
- **TypeScript** - 类型安全
- **MySQL2** - 数据库驱动
- **Crypto-js** - 加密库

## 🛠️ 开发指南

### 前端开发
```bash
cd frontend
npm run dev
```
- 前端运行在 `http://localhost:3000`
- 支持热重载
- API 请求会自动代理到后端

### 后端开发
```bash
cd backend
npm run dev
```
- 后端运行在 `http://localhost:3001`
- 支持 TypeScript 热重载
- 自动重启服务

### 类型定义
- 共享类型定义在 `shared/types.ts`
- 前后端都可以导入使用
- 确保接口一致性

## 📦 部署

### 构建生产版本
```bash
npm run build
```

### 启动生产服务
```bash
npm start
```

## 🔍 故障排除

### 端口冲突
- 前端默认端口：3000
- 后端默认端口：3001
- 可在配置文件中修改

### 数据库连接
- 确保 MySQL 服务运行
- 检查 `.env` 文件配置
- 测试连接：访问 `/api/health`

### CORS 问题
- 后端已配置 CORS
- 默认允许 `http://localhost:3000`
- 生产环境需要配置正确的域名

## 🎯 下一步计划

1. ✅ 完成基础架构搭建
2. 🔄 实现 API 接口
3. 🔄 完善前端组件
4. 🔄 集成数据库服务
5. 🔄 添加身份验证
6. 🔄 优化性能和安全性

## 📝 更新日志

### v1.0.0 (2025-01-01)
- ✅ 完成前后端分离重构
- ✅ 建立基础项目结构
- ✅ 配置开发环境
- ✅ 实现健康检查接口